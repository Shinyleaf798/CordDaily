# 设计决策记录

每做完一块功能就往下追加一条：改了什么、为什么这么改、放弃了什么。
每条都是独立的，不用从头读。

---

## 2026-09-09 后端骨架 + 登录注册

**改了什么**

- 建好 `backend/`：Express + Prisma + Neon 上的 PostgreSQL，目录按「路由 / 控制器 / 业务逻辑 / 中间件」分层
- 按规格文档把所有数据表建好
- 登录注册跑通：注册、登录、换新凭证、查当前用户。密码加密存，登录凭证 15 分钟过期
- 分类、账户两个模块做完整，其余模块先占位返回「还没实现」
- 所有接口统一返回 `{ success, data, error }` 这个形状

**为什么**

- 分类和账户逻辑最简单，先做完当模板给后面的模块照抄，也能尽早验证整条链路通不通
- 账户余额当场算出来，不存一个「余额」字段——存了迟早跟流水对不上
- 交易这些模块牵扯离线同步，没有手机端配合验证不了，先把接口路径和返回格式定下来

**放弃的**

- 参数校验没用 express-validator，改用 zod，写法更接近 TypeScript
- 记录的 id 不让数据库生成，必须手机端自己生成（核心原则之一）

---

## 2026-09-09 删用户时自动清掉他的数据

**改了什么**

给「用户 → 他名下的各张表」和「交易 → 交易的图片」这两类关系加上级联删除，其余关系保持原样。

**为什么**

- 以后要做注销账号，删一个用户应该自动清干净他的所有数据，不用一张表一张表手动删
- 图片是交易的附属品，交易没了图片记录留着没意义

**放弃的**

- 没给「分类 → 交易」「账户 → 交易」加级联：那样删一个分类会连带删掉底下几十笔账，是灾难。保持默认的「还有交易在用就不许删」，逼用户先处理那些交易
- 没给「周期规则 → 交易」加：删掉规则后，已经生成的那些账应该留着，只是不再跟规则关联

---

## 2026-09-09 升级 Prisma 7，后端改用新的 import 写法

**改了什么**

- Prisma 从 5 升到 7，连接数据库改走 `@prisma/adapter-pg`
- 数据库连接串从表结构文件里挪到单独的配置文件
- 后端约 20 个文件从 `require` 全部改写成 `import`
- 连真实数据库重跑了一遍注册/登录/建账户，顺便验证了上一条的级联删除确实生效

**为什么**

- 起因是编辑器提示连接串写在表结构文件里「已不再支持」——这是 Prisma 7 的硬要求，索性直接升上去
- 代码生成器选了旧的那个：新的会吐出混着 TypeScript 语法的文件，Node 直接跑会报错，这个版本还不成熟
- 改 import 写法工作量比预想大，但升到 7 之后不改就会一直处在「一半新一半旧」的别扭状态

**放弃的**

- 没用 `latest`（当时指向一个还没转正的版本），三个包都锁死同一个版本号
- 没为了用新生成器给项目加一层 TypeScript 编译流程：后端本来就是纯 JS，不值得

---

## 2026-09-09 补完后端剩下的模块

**改了什么**

交易、转账、周期规则、预算、统计五个模块全部实现完，占位工具删掉。连真实数据库跑了一整套测试：重复推送不会存两份、别人的分类塞不进我的交易、账户余额随交易增删实时变、两个统计接口的数字对得上。

**为什么**

- **批量推送交易时，插入前先查一遍哪些 id 已经存在**，而不是直接信任数据库的「跳过重复」：因为要精确知道这次新插了哪几笔，才能只给新的那几笔插图片。不然同步重试时图片会重复插一遍
- **周期规则遇到重复 id 直接返回已有的那条，不报错**：规则也是手机端离线生成的，网络重试不该让用户看到失败提示
- **统计接口只读不写**：手机端自己算超支提醒，服务器这两个接口只是把同样的算法在服务器上跑一遍，给电脑端画图用

**放弃的**

- 没做「父分类自动汇总子分类的花费」：规格文档没要求，猜错了返工更贵
- 没让改交易的接口顺便改图片：图片增删更适合单独做，塞进一个大接口里容易在「要不要先清空旧图」上出歧义

---

## 2026-09-09 手机端骨架：导航、本地数据库、登录

**改了什么**

- 用 Expo SDK 57 初始化 `mobile/`，用 TypeScript
- 本地 SQLite 建了 8 张表，字段照抄后端，但去掉 `userId`，加了一个「有没有同步过」的标记
- 登录做完：凭证存在手机的安全存储里，请求自动带上，过期自动换新再重试一次
- 底部导航做出「5 个按钮，中间凸起一个圆形 ＋」
- 「资产」这个 tab 做完整当模板，其余页面先占位

**为什么**

- **手机端用 TypeScript、后端用纯 JS，不一致是故意的**：Expo 模板自带 TypeScript，硬拆成纯 JS 是跟框架较劲
- **本地表不要 `userId`**：后端一张表服务所有人，必须靠它隔离；手机本地只服务当前登录这一个人，到处塞这个字段纯属浪费
- **底部导航用经典的 Tabs，不用新的原生 Tabs**：原生的塞不进一个自定义样式的凸起圆按钮
- **启动时读凭证加了容错**：一开始没加，读取失败时那个 Promise 没人接，App 会永远卡在白屏。现在读失败就退回「未登录」

**已知限制**

手机端在浏览器里跑不完整（安全存储在 Web 上缺方法），这是 Expo 官方的已知问题，不是这里写错了。这个 App 本来就不打算支持浏览器，不为它加兼容代码。

---

## 2026-09-14 三套可切换主题（白色·彩色 / 黑金 / 黑紫）

**改了什么**

- 配色表从「浅色 / 深色」两套改成三套具名主题，每套补了两个新颜色：卡片强调色，和「叠在强调色块上的文字该用什么颜色」
- 新增主题 store，选了哪套存在手机里，下次打开还是它
- 全仓库扫了一遍写死的颜色，全部改成读主题
- 「我的」页加了一排主题选择按钮，否则做完也没入口能切

**为什么**

- 三套里两套深色一套浅色，「跟随系统深浅色」这个模型表达不了，所以整个换成「用户自己选一个主题名」
- 每套主题的字段形状保持跟原来完全一样，这样只读 `theme.text` 的组件一个都不用改，改动收在三个文件里
- **「叠在强调色上的文字用什么色」必须由主题给**：黑金那套强调色是亮金色，白字看不清要用深色；另外两套白字没问题。让每个组件自己猜，黑金上线那天所有按钮都要返工

**放弃的**

- 没保留「跟随系统」当第四个选项：这三套都是用户主动挑的风格化配色，跟「自动跟随」这种工具型诉求不太搭
- 输入框那个半透明灰边框没收进主题：它跟配色无关，三套背景下都看得清

---

## 2026-09-15 记账表单四个字段：主题 / 备注 / 店名 / 地点

**改了什么**

- 交易表加「店名」「地点」两列，Prisma、本地 SQLite、后端校验三处同步改
- 主题跟金额并排在第一排，备注第二排，店名和地点收在一个可展开的区域里，默认折叠
- 店名和地点做历史补全（按出现次数排序），主题和备注不做

**为什么**

- **四个字段各管一个维度，不合并**：一开始我判断「标题」和「店名」重复、建议砍掉一个，是错的——实际用法里「标题」不是标题而是**事由**（麦当劳 / 请家人吃饭 / 3个汉堡2个薯条 / 吉隆坡机场 是四条不同的信息）。真正的问题是名字起错了，叫「主题」才不会被填成店名。**字段叫什么，直接决定用户往里填什么**
- **只有店名和地点做补全**：判据是「会不会被反复输入同样的值」。麦当劳会输入几十次，「3个汉堡2个薯条」永远只出现一次，给它补全反而碍事
- **店名直接存在交易表上，不单独建一张商家表**：建表就多一个同步接口，还多出「商家必须先于交易插入」的顺序依赖。而想要的补全和按商家统计，一条 SQL 分组就有了。等商家自己需要属性（默认分类、logo）再拆
- **店名地点默认折叠**：不是每笔都要记店家，四个输入框常驻会让面板太高，随手记一笔变得有负担。里面填过东西时图标变色加个小点，否则折起来就忘了

---

## 2026-09-15 打通本地保存 + 标签 / 报销 / 不计入统计

**改了什么**

- 记账页真正写进本地数据库，首页的月度卡片和账单列表接上真实数据
- 首次启动灌 13 个默认分类
- 报销从一个是/否，补成两个状态（待收回 / 已收回）
- 新增标签汇总页和报销清单页
- 本地数据库改成带版本号的升级机制

**为什么**

- **默认分类的 id 必须是真 UUID，而且只能写在代码里**：后端校验要求是 UUID，本地随便编的 id 存得进去，但**同步时整批交易会被后端打回**。而建表语句里生成不了 UUID，所以只能放在启动时用代码灌
- **本地数据库升级必须带版本号**：原来只有「表不存在就建」，没有版本概念。这次加列如果照原样写，**已经装过 App 的手机会永远停在旧表结构**，每次插入都报「没有这一列」，而且只在老设备上复现
- **报销必须是两个状态**：只有一个「可报销」的勾，打上就再也清不掉，待报销列表会越积越长直到没人看
- **勾报销时联动打开「不计入统计」，但放在界面上做，不在数据库层强制**：一开始写在保存函数里，结果界面显示「不计入统计 = 关」而库里存的是开，**等于界面在撒谎**。放在界面联动，用户看得见也能再关掉
- **三个口径故意不一致，这是设计不是 bug**：账单列表显示全部；月度统计和预算跳过「不计入统计」的；**账户余额不管这个标记**——钱确实出去了，余额必须真实
- **标签是跟分类平行的另一个汇总角度**：一趟「日本旅行」横跨机票（交通）+ 拉面（餐饮）+ 药妆（购物），按分类永远算不出这趟一共花了多少

**放弃的**

- 一度想把「标签」和「主题」合并：区别是主题每笔唯一、标签是可复用的归类键，前者是自由文本，后者存在的意义就是被拿来汇总
- 没在「标记已收回」时自动生成一笔收入：垫付的钱本来就没算进消费统计，再记一笔收入会让当月收入虚高

---

## 2026-09-15 本地预算 + 合并迁移文件

**改了什么**

新增本地预算读写和预算页，首页预算卡从写死的 800 换成真实数据；把本轮新增的两个数据库迁移合并成一个。

**为什么**

- **「设了预算但这个月还没花钱」的分类要留在列表里**（显示花了 0），不能因为没有对应交易就消失，否则用户以为预算没设上
- **预算口径的「已消费」跟月度总支出不是同一个数**：没设预算的分类不该算进预算消耗，否则预算卡永远显示超支
- **额度填 0 直接删掉那行**，不留一条额度为 0 的记录当噪音
- **输入框先存在本地草稿里，失焦才写库**：每敲一个数字就写一次数据库会触发刷新，输入框会跟着抖

**关于能不能改已经跑过的迁移**

只看一个问题：**它有没有跑到你控制不了的数据库上**。

- 只在自己的开发库跑过 → 随便改
- 已经推给同事 → 技术上能改，但要所有人重来一遍，通常不值
- **已经跑到生产上 → 永远不要改**，只能写新的往前修

这次是第一种，但库里有测试账号要保住，所以手工合并：新建合并后的目录沿用**较早那个时间戳**（目录名决定执行顺序），删掉被合并的两个，再从数据库里删掉那两条记录，最后告诉 Prisma「这条就当已经跑过了」。

关键认知：**迁移记录不是本地文件，它是数据库里的一张普通表**。所以「迁移文件」和「迁移记录」在两个地方，改一边必然对不上。它存在数据库里，是因为它记的是「**这一个特定的数据库**跑到第几步了」——同一份文件在开发库和生产库上可能各走到不同位置。

另外两条容易混的命令：改数据库结构的和只生成代码的是两回事，**互不依赖**。只跑后者会出现「编辑器里有自动补全，一跑就说没这一列」。

---

## 2026-09-15 首页预算环换成「预算 vs 时间」节奏条

**改了什么**

- 先在设计稿上出了 4 套方向对比，选了「节奏条」落地
- 全站金额统一千分位
- 环形进度换成一根横条，上面带一个「时间走到哪了」的刻度，再加一句「比时间快/慢 X%」
- 近 7 天账单从「一整块列表」改成「一天一张卡」

**为什么**

- **环形进度回答不了真正的问题**：环只说「花了 71%」。但月中花掉 60% 是超速，月末花掉 60% 是省钱——**同一个百分比，含义相反**。把预算进度和时间进度放在同一根轴上，一眼就看得出该不该收着点
- **快慢判断留 3 个百分点的死区**：不留的话月初每天都在「快 1%」和「慢 1%」之间来回跳，天天变但没信息量，人就不看了
- **支出金额不再标红**：账本里九成以上是支出，全标红等于整屏都在报警，反而看不出哪条值得注意。红绿留给当天小计和收入这种真正的例外
- **金额格式手写，没用系统的数字格式化**：手机上这个功能取决于打包时带不带语言包，同一份代码三端可能不一样。账本里数字对不齐很显眼
- **那根条不认识「预算」**：它只知道「一个进度 + 一个参考位置」，以后账户额度、报销进度都能直接拿去用

**放弃的**

- 大金环方案视觉最抓人，但首屏只放得下 3~4 条账单，没设预算的人第一屏还是空的
- 数据宫格方案信息密度最高，代价是没有主次——每个数字都一样重要就等于都不重要
- 极简账本方案最出效果，但没有卡片边界，哪里能点提示太弱
- 月份标签暂时不带下拉箭头：月份切换还没做，宁可先不画，也不放一个点了没反应的控件

---

## 2026-09-15 首页布局可切换（节奏条 / 金环）

**改了什么**

首页拆成「取数 + 分发」，两套布局各是一个组件，设置页加一个布局选择器。所有算出来的数字集中在一个函数里算好，布局组件只负责画。

**为什么**

- **算出来的数字必须只算一次**：日均消费、预算快慢这些库里都没存。如果每套布局各算各的，早晚出现「切到金环日均变了 0.01」这种没人查得出来的 bug。集中算一次之后，加第三套布局的成本就只是「一个组件文件 + 两行注册」
- **布局和配色是两个独立设置，不合并**：合并的话「极简账本」就跟黑金绑死了，想要「金环 + 白色主题」就没办法
- **布局偏好要在首屏之前读回来**：不等它读完就渲染，会先闪一下默认布局再跳成用户选的那套
- **列表行加一个「铺在什么底上」的属性，而不是复制一个组件**：两套布局的行只差图标块的形状和底色，文字完全一样，复制一份等于以后改文案要改两处
- **没设预算时，环中心退回显示「本月支出」**：环本身没意义时还写「本月还能花 RM0.00」是在撒谎
- **当前时间改成每次渲染时取**：原来是文件加载时取一次，App 挂后台过了零点再回来，「今天/昨天」和「这个月过了几天」全是错的

---

## 2026-09-15 components/ 按「谁在用」重新分层

**改了什么**

顶层 9 个散文件全部归进子目录，顶层不再直接放文件。首页取数包成一个 hook，同时导出一个不碰 React 的纯函数。

**为什么**

- **「乱」的根源不是文件多，是顶层同时躺着四类东西**：基础件、通用画图件、有业务含义的展示件、app 骨架。四类各有各的归属规则，混在一层看起来就是随机的。所以规则定成两条：顶层只放目录；目录名回答「谁在用」
- **判断一个件进 `ui/` 还是进页面目录，看它认不认识业务概念，不看现在谁在用**：那根进度条只知道「一个进度 + 一个参考位置」，不认识「预算」，所以进 `ui/`——哪怕目前只有首页用。放进 `home/` 的话，以后账户额度要画环就得从首页目录里 import，那才是真的乱
- **交易行单独一层**：它目前只有首页用，按「谁在用」该进 `home/`。但它描述的是「一笔交易怎么显示」，日历页一定会用，放进 `home/` 会逼着日历页从首页目录里 import，这是最典型的「目录在撒谎」
- **算数的纯函数单独导出**：以后要给「预算快慢」这类规则补测试，喂一组假数据给它就够了，不用渲染组件、不用起数据库

**踩到的**

批量替换 import 路径时漏掉了两行相对路径的写法，类型检查当场报出来了。**这类纯搬家的改动，类型检查就是安全网**。

---

## 2026-09-15 首页「还没设置预算」变成可点入口

原来两套布局都只是一行灰字，点不了。抽成一个组件，点了跳去设预算。

- **抽成组件而不是两套布局各写一遍**：「点了去哪」只写在一个文件里，以后改目的地不用改两处
- **可点范围靠 `hitSlop` 撑到 44，不靠加内边距**：加内边距会把卡片版式撑开，`hitSlop` 只扩大触摸范围不影响布局

---

## 2026-09-15 预算改成「填一个总数就完事」

**改了什么**

预算从「按分类各设一个额度」改成「填一个月度总数」，存在本地设置表里，不进服务器数据库。按分类设额度那一页和后端整套预算代码都删掉了。

**为什么**

- **分类预算的颗粒度太细**：要先想清楚「餐饮该给多少」才能用上这个功能，大部分人根本过不了这一关。一个总数是立刻能填、立刻有用的
- **口径必须跟着换**：分类模式下「已消费」只统计有预算的分类。总预算管的是所有花销，还沿用那个口径的话，一个分类预算都没设过的人会永远看到「已用 0%」
- **这不跟「账户余额是算出来的」那条原则冲突**：余额是能从流水算出来的量，存了会不一致；**预算是用户直接给的输入，本来就没有别的地方能算出它来**，不存反而没地方放
- **只有一种模式生效，不做「两个都算取较小值」**：预算提醒的价值在于一眼能信，规则一复杂就没人信了
- **填 0 = 清除**，不留一条额度为 0 的噪音

**已知代价**

预算存在本地，不参与同步，所以**换手机或重装 App 会丢**。等做「设置同步」时一起解决，现在不值得为一个数单开一条同步链路。

**本地删表也有讲究**：老版本建表的那组语句不能拿走。新设备走「建表 → 删表」，老设备走「建表（早就跑过）→ 删表」，两条路径的终点必须一样。

---

## 2026-09-17 分类图标、分类管理页，记账页补上日期和账户

**改了什么**

- 分类图标支持三种写法：表情、App 内置图片、用户自己的图片，统一存成一个带前缀的字符串
- 新增分类管理页（改名 / 换图标 / 删除 / 新建），两个入口：记账页分类网格最后那格「设置」，和「我的 → 分类管理」
- 记账页补上日期和账户两个选择器，月历是自己画的

**为什么**

- **图标用一个带前缀的字符串，不拆成两列**：这个字段只有「渲染一个图标」一个用处，没有任何查询要按类型过滤。拆成两列换来的是每张表、每次插入、每个同步内容都多一个字段，收益是零
- **内置图片存的是「引用」不是文件名**：以后换图、改文件名、甚至把 PNG 换成 SVG，都不用动数据库里的任何一行
- **必须手写一张图片登记表**：打包工具靠扫描代码里写死的路径决定哪些图片打进包，拼接出来的路径它看不见，运行时必然报错。所以「往文件夹里一丢就自动生效」在 React Native 里做不到，登记表是这条限制的产物，不是啰嗦
- **被引用的分类删不掉，按钮直接灰掉**：后端本来就不许删，本地要是允许删、同步时会被打回，用户看到的是「删掉的分类又回来了」这种没法解释的现象。灰按钮加一行「N 笔记录在用」，比让用户点了才知道不行要好
- **「设置」固定在分类网格最后一格**：想改分类的念头几乎只在「扫了一遍没找到合适的」时冒出来，那一刻视线正停在网格末尾
- **月历自己画，不用系统的日期控件**：系统控件两端长得完全不一样，也不吃这个 App 的配色——在一屏自定义配色里弹出一个系统灰的转轮像走错了 App
- **弹层不再一律开路由**，判断标准是**这一层有没有自己的地址**。选日期、选账户、选图标都是「挑一个东西还给调用方」，走路由就得为一次性的结果多建一个 store，或者把值塞进网址参数再解析回来——都是为了绕开「组件之间传个值」这件本来很简单的事

**放弃的**

- 没做「从相册选分类图标」：要引入一套本地文件的生命周期管理，等接图床直传时一起做
- 删除没做成「软删除」：分类没有历史价值，留一个删除标记只会让每个查询都要记得过滤它

---

## 2026-09-17 首次启动灌一个默认账户「现金」

**改了什么**

空库时灌一个「现金」账户，已经有账户的设备不受影响。

**为什么**

- **这是在补一个一直存在的洞**：交易必须有账户，没有账户就一笔账都记不了。全新安装的 App 打开后保存按钮永远是灰的，界面也没地方解释为什么。从来没人碰到过，只是因为开发机上早就手动建过账户
- **只灌一个，不灌三个**：灌三个等于每笔账都要**做一次选择**。账户对不关心它的人应该是完全隐形的——一个默认值一直在，永远不用点开
- **没有把「账户」改成可选**：那要动本地表、后端校验和每一条统计查询，还多出「没有账户的交易」这个新状态。灌一行数据便宜得多
- **期初余额给 0**：一上来就逼用户盘点钱包里有多少现金，是在记账之前先设一道门槛

**顺带记一笔：账户这个概念现在的处境**

盘过一遍——余额没有任何地方在用；转账表只有底层代码认识，一个界面都没有；资产页存在的唯一目的是让记账页那个必填字段有值可填，属于自我循环。

**结论是留模型、砍存在感**，不是删表：拆的代价远大于留的代价，而且以后想加回来更贵——历史交易得回填账户，那时已经不记得哪笔是现金哪笔是卡了。对账（「这个月信用卡刷了多少」）是迟早会来的真实需求。

---

## 2026-09-17 记账保存不再默默失败

**改了什么**

保存键为什么是灰的，界面上直接说出来（先选分类 / 先选账户 / 金额还是 0）；保存失败留在原地不跳转；保存成功一路关回首页。

**为什么**

- **起因是一个查了半天的「点保存没反应」**：库里一个账户都没有时保存键是个死按钮——不跳转、不报错、什么都不发生。用户唯一能得到的信息是「按钮好像坏了」，而真正的原因界面从头到尾没提过。**一个禁用的按钮必须说明它为什么禁用**，否则它和一个坏掉的按钮在用户眼里没区别
- **一次只报一条待办**：三条一起列出来，人不知道先干哪个，反而更难动
- **失败不跳转**：记账这件事一旦让人以为记上了，就不会有第二次。跳转本身就是一种「成功了」的信号，所以它必须只在真成功时发生
- **保存后回首页而不是「返回」**：＋号是全局动作，不属于任何一个 tab。从资产页按的＋，「返回」会把人送回资产页，那笔刚记的账落在哪儿完全看不见

**没有改的**

首页只显示近 7 天，补记的旧账不出现在那里——这是设计不是 bug，那张卡本来就叫「近7天账单」。

---

## 2026-09-17 账单详情层（编辑 / 复制 / 删除）

**改了什么**

点首页任意一笔弹出底部详情层，底部一行三个动作。记账页拆成两层：外壳只负责「要编辑的那笔查出来没有」，表单本体新建和编辑共用。记账页转场从「从下弹出」改成「从右滑入」。

**为什么**

- **详情是底部弹层，不是整页**：看一笔账是「瞄一眼确认是不是这笔」，不是一次阅读。盖掉整屏会让人失去「我刚才在列表的哪个位置」的感觉
- **删除确认不叠第二层弹窗，在同一层里换内容**：弹窗套弹窗在 iOS 上会有里层关掉外层跟着闪一下的毛病；而且「确认删除」本来就该接管这一层
- **复制出来的那笔日期落在此刻，不抄原来那天**：复制的用途是「今天又买了一样的东西」。照搬旧日期的话，复制一笔三周前的账，首页什么都不会发生，用户只会以为按钮坏了——跟刚修掉的「点了没反应」是同一种伤害
- **外壳等数据到齐了才挂载表单**：这样表单能直接拿数据当初始值。先渲染一轮空表单再填值的话，编辑时肉眼能看见闪一下
- **弹层由首页持有，不放进布局组件**：两套布局点同一行应该弹出同一个东西，放进布局里就变成两份要同步维护的实现
- **不可点的列表行退回普通 View，而不是给可点组件传「禁用」**：禁用的可点组件仍然会吃掉触摸事件，外面包着的滚动区域会跟着变迟钝

**已知代价**

- **删除是真删行，没有留痕**。这笔要是已经同步到服务器了，本地删掉、服务器上那份还在——单向同步没有「删除」这个动作。等做同步时要补一张「被删的 id」表推给服务器。现在服务器上一条数据都没有，先建表不划算，但**别忘了这条**
- **编辑时「主题」回填的可能不是用户当初填的**：主题为空时会用店名或分类名兜底存进去，库里只有这一个字符串，分不出它当初的来源

---

## 2026-09-17 日期选择器加时分，详情里日期写全

**改了什么**

日期选择器加小时/分钟两条横向数字带，默认收起；选择器从「点一天就关」改成「改草稿 + 按确定生效」；详情里日期写成完整的「昨天 9月16号 16:00」。

**为什么**

- **加了时间就必须改成「草稿 + 确定」**：点一天立刻关闭的话，根本没机会再调时分；而「先调时间再点日子」这种隐含的操作顺序没人猜得到。代价是常用路径多一次点击，换来的是不存在「顺序错了就白调」这种坑
- **时间默认收起来**：绝大多数记账就是「刚刚」，默认值取当前时刻，不展开也永远是对的
- **分钟给满 60 个，不做每 5 分钟一跳**：横着滚一条数字带，60 个和 12 个操作成本差不多，但少了「我那笔是 16:07，只能选 16:05」的将就
- **日历和时间带包进滚动区，底部那行留在外面**：弹层超出部分是直接裁掉的——小屏上时间带一展开，被裁掉的恰好是最下面的「确定」，那就又造出一个「点了没反应」的按钮
- **详情里日期要三样全给**：只说「昨天」答不出几点，只说「9月16号」答不出那是前天还是上周。相对词负责快速定位，绝对日期加时刻负责说准
- **删除按钮不用红底白字**：三套主题里有两个的红是浅红，白字压不住。红字配普通底色一样刺眼，还没有可读性问题

---

## 2026-09-17 页面的「底线外观」写在路由层，不写在组件里

**起因**

进记账页点左上角 X 关闭时，会闪一下**白底、标题写着 "Add"** 的空屏。拆开看是两件事凑在一起：

- **标题退回路由名**：真正的标题是表单组件内部设的，**那份设置跟着组件走**。组件不在的那几帧（转场中、编辑模式还在查数据），标题就退回默认的路由名
- **底色退回导航器自带的白**：表单最外层没铺底色。平时看不出来，是因为白色主题的背景跟它几乎一样——这其实一直是个隐患，黑色主题下这个页面的底色也不归主题管

**所以规矩是**

一个页面的「底线外观」（标题、底色）写在路由那一层，组件里那份只加它额外要的东西。组件里设的选项天然是有条件的（组件挂载了才算数），拿它兜底等于假设「组件永远在」，而转场和异步取数这两种情况下它就是不在。

**光补标题和底色还不够，导航器自己的配色才是根**

第一版补完，截图回来标题对了、**底色还是纯白**。关键线索就是那个白：**纯白不是任何一套主题的颜色**，那是 Android 原生窗口底色，说明那一层压根没有任何背景在画。

真正的根是：**导航器自己有一套颜色，从来没跟 App 的三套配色接上**。屏幕底色、顶栏底色、返回箭头、标题文字全归它管，而它默认只有浅色/深色两档。所以改成用 App 的配色拼一套喂给它，底色画在更外面那一层，内容一个都不剩时也还在。

---

## 2026-09-17 从原生栈换到 JS 栈

**改了什么**

页面导航从原生实现换成 JS 实现（`expo-router/js-stack`）。没装新依赖——expo-router 自己就带着一份，而且明说了「App 代码应该用这个」。

**为什么**

直接起因是想自己写转场动画。但真正非换不可的，是原生栈上一个改不掉的现象：**页面关闭时内容先被清空、再滑走**——看到的是一块纯色面板带着顶栏滑出去。

根因查到底了：关闭的一瞬间，那条路由**当场从路由表里消失**，React 立刻卸载整棵子树，而原生层这时才开始播关闭动画——**滑走的必然是空壳**。

JS 栈没这个问题，因为它不认为「路由表里有什么」就等于「该画什么」：它自己记着一份「正在关闭的页面」，**动画播完才真正移除**。同一件事，一个交给原生去播、React 侧提前撒手，另一个自己从头管到尾。

**放弃的**

- **改动画参数**：无关。滑动本身一直是好的，坏的是内容存在与否
- **用底色兜住**：那层 View 跟着一起被卸载，兜不住（上一条已经踩过一遍）
- **升级 expo-router**：新版里那段代码逐字节相同
- **拦住返回动作不让它卸载**：实测**能**拦住，但要配一套透明弹层 + 自绘动画 + 手搓侧滑手势，而且那条路自己也得关掉原生动画——**等于同样是 JS 动画，却多付了内部 API 和手搓手势的代价**。这是选 JS 栈的决定性理由

**已知代价（接受）**

动画由 JS 驱动而不是原生线程；顶栏从原生控件换成 JS 渲染——这个项目的顶栏本来就全是自定义的，不受影响。

---

## 2026-09-18 「我的」拆成四格网格 + 四个二级页

**改了什么**

从一长条卡片列表改成四个圆形图标（账本 / 布局 / 主题 / 其他），每格跳一个二级页。账号信息和退出登录挪进「其他」。

**为什么**

- 原来那一页把四类互不相干的设置堆在一个滚动列表里，越往下加越难找。**网格的优势不是好看，是四个选项一屏内全部可见**——跟记账页的分类网格同理，都是「扫一眼挑一个」的场景
- 格子宽度用 25% 而不是分类网格的 20%：分类有十几个，20% 才排得下一行五个；这里只有四个，25% 正好铺满
- 四个入口都开路由而不是弹层，判断标准还是**这一层有没有自己的地址**：它们可以被直接跳进来、该出现在返回栈里、按返回键该退回「我的」，而且不用往回传值
- 主题页每项左边加一条配色预览：三套主题真正的差别就是那一个强调色，光看「黑金 / 黑紫」这种名字选不出来

**放弃的**

没把邮箱留在顶部当身份标识：邮箱一年确认一次、登出更少，留在首屏等于用最显眼的位置放最少用的东西。

---

## 2026-09-18 日期一律走本地取值，不从 ISO 字符串里抠字符

**改了什么**

三处「切字符串取日期」的写法全部改掉，改用 `Date` 的本地取值方法。

**为什么**

`toISOString()` 永远把时刻渲染成 **UTC**。东八区比 UTC 快 8 小时，所以**本地 00:00–08:00 这段时间里 UTC 还停在昨天**，切出来的年月日是「UTC 眼里的那一天」。

两种切法严重程度差很多：

- 切到「日」→ **每天都错 8 小时**，1/3 的时间
- 切到「月」→ 只有每月 1 号的 00:00–08:00

所以报销清单那处是真被看见的 bug（凌晨记的账全显示成前一天），而另外两处的症状是 9 月 1 号凌晨首页先闪一下 8 月的汇总——因为那 8 小时里算出来的 key 跟上个月一模一样，缓存直接命中了上个月的数据。

更要命的是**切完就救不回来了**：完整的 `2026-08-31T19:00:00.000Z` 里既有时刻也有基准，转到任何时区都行；切成 `2026-08-31` 之后 19:00 和那个 `Z` 都没了，而进不进位恰恰由那个 19:00 决定。

**规矩一句话：ISO 字符串是给机器存的，`Date` 的取值方法是给人看的，中间那一步不能省。**

**为什么之前没抓到**

代码顶上早就写了这条警告，但只点名了「切到日」那种写法。「切到月」是同一个坑换了个切法，又藏在缓存 key 这种看不见的地方，就溜过去了。

---

## 2026-09-18 二级分类，以及分类网格里两个「等分」的坑

**改了什么**

分类网格只画一级，有子级的格子右下角带一颗小点，点开在它下面浮出一个二级面板。面板第一格是父分类自己，点面板外面也等于选中父分类——否则有子分类的一级分类永远选不中。

**坑① `width: '20%'` 排不下 5 个**

1/5 在某些屏宽上除不尽，每格四舍五入到整数像素后，5 格加起来超过容器宽度，第 5 格被挤到下一行。日历那边 1/7 踩过完全一样的坑（表头变成「日一二三四五 / 六」）。

改成一行是一个真的横向容器，每格平分剩余空间。最后一行不足要补空格子，否则剩下那两三个会摊开占满整行。

**规矩：要等分就别用百分比，用真的行 + 平分。**

**坑② 选中的圆在真机上画成方块**

A/B 实测过：把圆角和底色写在**装着图标和那颗点**的容器上 → 方块；换成一个独立的空容器 → 正圆。

原因：圆角的含义是「把这个容器连同里面的内容一起裁成圆角」。而那颗点是**故意伸出边界**的，一裁就会被削掉。系统于是不裁了——不裁就等于没有圆角，背景退回矩形。

注意限定：**圆角容器可以装东西，只是装的东西不能越界**。图标待在里面完全没事，问题只出在那颗骑在边上的点。

**规矩：一个容器要圆角，里面的东西就得待在圆里；有东西非要伸出去，就把圆角挪到另一个容器上。**

**为什么这个 bug 查了很久**

它有三层伪装，每一层都指向错误的方向：

- 「**存盘时是圆的，点一下别的分类就变方**」→ 像是运行时重建了背景。其实形状从第一帧就是方的，只是没选中时背景透明、看不见
- 「**面板里是圆的，网格里是方的**」→ 像是两边样式分叉了。其实是同一套样式，差别在面板里的格子**没有那颗点**
- 选中的恰好是 📚 🏠 这类本身就方的表情，一度以为看到的是表情自己

真正定位它的是那句「点开有子级的，里面是圆的、关掉外面是方的」——那是唯一一次把「有没有那颗点」和「圆还是方」直接对上。

**教训：形状只跟结构有关，跟状态无关。一开始就该去比较两处的结构差异，而不是去找「点击时发生了什么」。**

---

## 2026-09-22 消费日历：月历格子 + 就地展开当天明细

**改了什么**

日历页从占位变成真页面，三块：

- 顶部一张卡：月份左右切换（翻远了出现「回到本月」）、本月消费大数字、日均消费和本月收入
- 中间月历：一格一天，格子上写当天消费，花得越多底色越深，今天描一圈边
- 点某天，**在同一页**下面展开那天的全部账单；再点一次收起

数据上加了 `listMonthTransactions`（一次取一个月）和 `useMonthTransactions`（按月缓存），
派生全在 `buildCalendarViewData` 里，跟首页的 `buildHomeViewData` 同一个路子。

**为什么点一天不跳页**

日历的用处是**横着比**——今天点一下看完，顺手点旁边那天比一比。
跳到独立的「某天账单」页再退回来，比较两天要点四次，日历就退化成一个花哨的日期选择器了。

**为什么每天的小计在 JS 里算，不写 `GROUP BY substr(date,1,10)`**

库里存的是带时区的 ISO 字符串，按字符串切出来的是 UTC 日期，东八区凌晨 8 点前记的账会被算到前一天
（9-18 那条记的就是这个坑）。SQLite 的 `date(..., 'localtime')` 又依赖设备时区设置，
跟 App 其它地方 `getFullYear/getMonth/getDate` 的口径不保证一致。
一个月几十到几百条，取回来在 JS 里分天的开销可以忽略，换来全 App 只有一套「哪天」的算法。

**两套口径同时存在，是故意的**

- 格子上的数字、顶部总额：**跳过**「不计入统计」的交易，跟首页和预算一致
- 展开的当天明细：**显示全部**交易，记了就该看得到

由此多出第三种格子状态：有账但合计是 0（整天都是垫付待报销这类）。
这种格子日期下面点一个小点——不留记号的话用户会以为自己那天没记账。

当天明细标题上的小计跟着格子走（也跳过不计入统计的），哪怕这跟首页的当天小计差一点：
点开一个写着 50 的格子、下面标题却写 80，比两处都不准更糟。

**日均的分母**

看本月就只除已经过完的天数，看过去的月份就除整月天数，并且把分母直接写在界面上
（「日均消费 · 已过 22 天」）。月初 3 号拿 30 天当分母，会算出一个假的「很省」。

**放弃的替代方案**

- 一次拉全部交易在前端切月：翻月是瞬时，但首次进页面要等全表；按月查 + 按月缓存，翻回去同样是瞬时的
- 格子用连续渐变表示金额深浅：人眼分不出 38% 和 44%，分成四档才看得出「这周有两天明显深」
- 格子里画上/下个月的日期（灰色）：点下去要么跳月要么没反应，翻月已经有箭头了，不该再冒出第二条路径

**顺手做的**

「今天 / 昨天 / 9月13日 + 副标题」这套分组标题的算法本来长在 `use-home-view-data` 里，
日历要用同一套说法，挪进了 `utils/date.ts`。两份各自演化的话，
同一天在首页叫「昨天」、在日历页叫「9月21日」，看起来像两个不同的东西。

---

## 2026-09-22 账单金额的显示口径：颜色不当标签，六位数起缩写

**改了什么**

四件事，都是"一笔钱在界面上怎么写"：

- 当天小计从红绿两色的 `+RM5,000.00 / -RM412.90`，改成「收 RM5,000.00」「支 RM412.90」
- 每一行账单的金额不再按收支染色，一律用正文色，靠 `+/-` 号分方向
- 行内金额补回货币符号：`-RM12.50`
- 六位数（十万）起缩写成 k，最多两位小数、末尾的 0 不写：`600000` → `600k`，`597888` → `597.88k`

**为什么颜色不再当标签**

原来的规矩是"支出用正文色，收入染绿，红绿留给例外"。前半句依然成立，
但把例外单独染色带来了另一个问题：一屏账单里零星几行绿字，眼睛会先被它们抓走，
而它们恰恰是最不需要盯着看的那几笔。

现在是进是出改由字说——行内是 `+/-` 号，当天小计是「收/支」两个字。
颜色腾出来只表达一件事：这是不是正文。

注意「白色」不能写死 `#ffffff`：白色主题的卡片底色就是白的，写死等于看不见。
用的是正文色 token，深色主题下是白的，白色主题下是黑的。

**为什么缩写门槛定在六位数**

早先是一千就缩（`1842.5` → `1.8k`），但日常一天花一两千是常态，
满屏 `1.2k` `0.8k` 反而要在脑子里换算一遍才知道差多少。五位数以内写完整数字，一眼能比大小。

判断够不够六位，用的是**四舍五入之后**的整数值：`99999.6` 按常规写法会显示成 `100,000.00`，
那正是这条规则要避开的六位数，所以它得走 k 这一支。

小数**截断**不是四舍五入（`597.888` → `597.88`）：缩写本来就是在丢精度，
丢的方向宁可让显示的钱比实际少几分，也不要凭空多出来。

**规则只写一遍**

`toCompactK` 放在 `formatAmount` 里，`formatCurrency` 和 `formatSignedAmount` 都调它，
日历格子的 `formatCompactAmount` 也共用——"多大的数开始缩写"全 App 只有一处定义。
首页的本月支出、日历的当天小计、每一行账单，六位数以上必然是同一个写法。

能这么做的前提是 `formatAmount` 只用于显示：录入框的预填走的是 `String(amount)`，
不经过这里，所以缩写不会被当成输入值再解析回去。

**留下的不一致**

账单详情弹层里那个大金额还是绿的（收入）。那是单条详情，不在"一屏账单"的语境里，
暂时没动——但列表不绿、点进去变绿，早晚要统一。

---

## 2026-09-22 删除资产账户：默认账户删不掉，账单转移而不是跟着删

**改了什么**

- 资产页每个账户右边多一个删除按钮，点了弹确认框
- 默认账户（首次启动灌进来的那个「现金」）不给删除按钮，改成标一个「默认」
- 删掉的账户名下的账单**转到默认账户**，不跟着删
- 顺手统一了四个 tab 页的标题字号

**为什么默认账户必须删不掉**

`transactions.accountId` 是 NOT NULL。一个账户都不剩的话，记账页的保存按钮会永远是灰的，
而界面上没有任何地方解释为什么——这正是 seedDefaultAccount 当初存在的理由。

一度考虑过"让账单可以不属于任何账户"（把 accountId 改成可空），放弃了：
那要改本地表、Prisma schema、后端校验和每一条聚合查询，还凭空多出一个
"没有账户的交易"的状态，所有统计都得重新想一遍怎么对待它。
留着最早那个「现金」当兜底便宜得多，而且用户根本不需要知道有这回事。

**怎么认定哪个是默认账户**

建得最早的那个，不加 `isDefault` 列。那会是一个要跟着同步、还要保证
"有且只有一个为真"的新状态；而"最早的那个"本来就唯一、本来也不会变。

排序是 `createdAt ASC, id ASC`——带上 id 是为了确定性：灌完默认账户后立刻手动建一个，
两条的 createdAt 可能落在同一毫秒，光按 createdAt 排，SQLite 给的先后是任意的。
而"列表第一行"正是界面认定默认账户的依据，它必须每次都指向同一行。

界面不给删除按钮，数据层再拦一道（`deleteAccount` 直接抛错）。规则写在数据层才是真的删不掉。

**为什么账单是转移不是删除**

用户删的是「招商银行」这个**容器**，不是"我上个月花的那三百块"。
跟着删掉的话，月度支出会凭空少一截，而他完全不会把这件事跟刚才那次删除联系起来。
确认框里直接写明"这个账户下的 N 笔账单会转到默认账户，账单本身不会丢"。

转账行则是直接删掉：一笔转账的两端都必须存在，一端没了就不成立，
改指到默认账户会变成"自己转给自己"，还会悄悄改掉余额。
（目前没有任何地方能创建转账，这张表一直是空的，这段是为以后接上转账功能时不留坑。）

整段包在一个事务里，中途失败不会留下"账单已经转走、账户还在"的半截状态。

**顺手：页面标题字号**

首页、日历早就各自写 `fontSize: 28` 覆盖 `type="title"` 的 48，资产和「我的」漏了，
就孤零零地大一圈。与其复制第四遍，给 ThemedText 加了一种 `pageTitle`，四个 tab 页共用。

---

## 2026-09-22 账户隐形：只有一个账户时，界面上当它不存在

> **这条已作废**，同一天被下面「两个默认账户」那条取代。
> 隐形解决的是「看不见」，但用户真正要的是「能明说我不分账户」——
> 藏起来的话，那笔账实际上还是记在「现金」名下，界面在替他做一个他没做过的断言。
> 留着这条是为了记下这条路走过、以及为什么掉头。

**改了什么**

- 记账页：只有一个账户时不画账户 chip
- 详情弹层：同上，不显示「账户」那一行
- 资产页：期初余额是 0 的账户，那个数字改叫「净流水」而不是「余额」；金额改走 formatCurrency
- 资产页只有一个账户时，标题下面写明"账户是可选的"

**起因**

用户问"能不能不选账户也能记账"。查下来**本来就可以**——记账页取的是
`pickedAccountId ?? accounts[0].id`，不碰那个 chip 也能保存。真正让人觉得麻烦的是另外两件事：
chip 一直杵在那儿像个待办事项，以及资产页那个会越变越负的数字。

**为什么是"隐形"而不是"把 accountId 改成可空"**

可空要改本地表迁移、Prisma schema、后端校验和每一条聚合查询，还会带来一个新的不一致：
资产页的总额和首页的收支总额从此对不上，而界面上没有地方解释为什么。

更关键的是**它治不好这个病**：就算账单可以不属于任何账户，资产页还在那儿，
用户照样会觉得"我是不是该维护它"。负担来自界面，就该在界面上解决。

**为什么条件是"只有一个账户"，而不是一个设置开关**

这个条件自解释：去资产页加第二个账户，chip 自动出现；删回一个，又自动消失。
用户不需要先知道有个开关存在，也不用去找它在哪。

判断写成 `accounts.length !== 1` 而不是 `> 1`：一个账户都没有时（理论上不该发生，
默认账户删不掉）得把 chip 露出来，否则保存被拦下时用户看不到任何可以操作的地方。

加载中（`accounts` 还是 undefined）当作隐形：宁可晚一帧出现，也不要闪一下又消失。
连带修了一个自己引入的不一致——`blockerHint` 也要加 `accounts &&`，
否则加载中的那几毫秒会显示"先选一个账户"，而 chip 正因为同样的未知状态藏着，
提示指向一个不存在的按钮。四种状态（加载中/0个/1个/2个）的自洽性验过。

**「余额」改叫「净流水」**

余额是衍生值（原则#6）：openingBalance + 收支汇总。没填期初、又只记支出的人，
这里会显示 -1,234.00 并且越记越负——看着像坏了，于是觉得"我得把它弄对"。
而它从来不需要维护，它只是"记账以来进出相抵"。同一个数，说清楚它是什么，压力就没了。

---

## 2026-09-22 两个默认账户：「不选择任何账户」+「现金」，默认值沿用上次的选择

**改了什么**

- 撤销上面那条「隐形」：账户 chip 和详情里的账户行都恢复常驻
- 首次启动灌**两个**账户：「不选择任何账户」和「现金」
- 记账页的默认账户 = **上次记账用的那个**，不再是列表第一个
- 兜底账户从「现金」换成「不选择任何账户」：它删不掉，删别的账户时账单转到它名下

**为什么这个比隐形好**

隐形解决的是"看不见"，但用户真正要的是**能明说"我不分账户"**。
藏起来的话，那笔账实际上还是被记在「现金」名下——界面在替他做一个他没做过的断言。
给他一个叫「不选择任何账户」的真账户行，这句话就变成他自己说的了。

代价还更小：它就是一个普通账户，`accountId` 继续 NOT NULL，
本地表、Prisma schema、后端校验、每一条聚合查询全都不用动。

**为什么默认值是"上次用的"而不是固定某一个**

两个默认账户摆在那儿，固定选哪个都是错的：选「现金」等于逼不想分账户的人每次改，
选「不选择任何账户」等于逼想分账户的人每次改。
用"上次用的"，两种人都只需要选一次。

实现上不另存一个"上次选择"的设置，直接从**最后一条记录**（createdAt 最新）反查账户。
那本来就是"上一次的选择"，不会跟真实情况走散，也不用在保存成功后再写一次库。
跟 getLastCategoryForMerchant（记住上次在这家店选的分类）是同一个路子。

queryKey 挂在 `['transactions', ...]` 下面：记完一笔，use-transactions 的 invalidateAll
会把这棵整个失效掉，下次打开记账页自然拿到刚用过的那个，不用额外做任何事。

**为什么兜底账户换成它**

删掉「招商银行」时把那些账单转成「现金」，等于替用户断言"这些钱是现金付的"——那是假的。
转成「不选择任何账户」才是真话：这些账还在，只是不再归属于任何账户。

**怎么记住哪一行是「不选择任何账户」**

它的 id 存在 `app_settings` 里，没有在 accounts 表上加 isDefault 列，也没有写死一个固定 id。

写死固定 id 本来最省事（迁移老用户一条纯 SQL 就够），但 `account.id` 是**全局**主键，
所有用户的数据最终进同一张服务器表，写死的话两个用户一同步就撞 id。
`app_settings` 则天然是本机的事，这张表本来就不进服务器。

**两条 seed 的时机不一样，是故意的**

- 「不选择任何账户」看的是**它自己在不在**，所以老用户升级上来也会补，而且它删不掉
- 「现金」只在**全新安装**时灌一次。老用户可能已经把它删了，不该每次启动又长回来
  （分类那边踩过同样的问题，解法一样）

由此带来一个界面上的坑：全新安装时它排第一，老用户是后补的、排在最后。
**位置靠不住**，所以资产页判断"哪一行不给删除按钮"是按 id 比对，不是 `index === 0`。

验过的场景：全新安装、重复启动不重复灌、老用户补齐、删过现金的库不会把现金种回来、
上次用的账户跟着最后一笔记录走。

---

## 2026-09-22 资产页重做：净资产总览 + 按类型分组 + 新建/编辑

> **这条的大部分已作废**，被同一天最后「资产砍回最低限度」那条推翻：
> 净资产卡、已报销/未报销卡、分类小计、可折叠分组都已删除。
> 仍然有效的只有两点：余额改成一条查询算完（listAccountBalances），
> 以及缓存 key 挂在 accountBalance 下面的理由。留着是为了记下这条路走过、以及为什么掉头。

**改了什么**

照参考截图重做了资产页：

- 顶部净资产卡（强调色铺底）：净资产一个大数字，底下拆成总资产 / 总负债，带一个遮金额的眼睛
- 已报销 / 未报销一张小卡
- 账户按类型分组，每组一张卡：组名 + 个数 + 组内小计 + 可折叠
- 点一行编辑，底部 `+` 新建，删除入口在编辑弹层最下面
- 数据层加了 `listAccountBalances`（一条查询算完所有余额）和 `updateAccount`

**总资产/总负债按余额正负分，不按账户类型分**

信用卡刷爆了是负债，还多了钱就是资产；现金账户被记成负数（期初没填、只记支出）同样是负债。
按类型硬分的话，一张没欠钱的信用卡会被算成负债，那是假的。

**为什么余额要改成一条查询**

分组小计和总资产都需要**所有**账户的余额。原来每行自己调 `getAccountBalance`，
那个函数内部有四条子查询，N 个账户就是 N×4 条；而且小计得等所有行都回来才能算。
改成三个 GROUP BY 子查询 LEFT JOIN 回账户表，一条搞定。

算法必须跟 `getAccountBalance`（以及后端 account.service.js）逐字一致：
openingBalance + 收入 - 支出 + 转入 - 转出。两处一旦分叉，就会出现
"这一行显示 100、小计里却按 90 算"这种没人查得出来的 bug。写完拿真 SQLite 对跑过两种算法。

跟统计口径不同，余额**不**跳过 excludeFromStats：那笔钱是真的离开了账户，
"不计入统计"说的是它不该占用预算额度，不是它没发生过。

**缓存 key 挂在 accountBalance 下面，不是 accounts 下面**

余额会被**记账**改变，而 use-transactions 的 invalidateAll 里失效的正是 `['accountBalance']`。
挂到 accounts 下面的话，记完一笔账资产页的数字不会变——那种 bug 很难联想到缓存 key 上。
顺带修了一个原有的漏失效：新建账户会带着期初余额改变总资产，原先只失效了账户列表。

**没做的两块，以及为什么**

- **借入 / 借出**：需要一套借贷功能（谁借的、什么时候还、还了多少），数据库里没有任何一张表
  能回答这些。画两个永远是 RM0.00 的格子比不画更糟：它看起来像功能坏了，而不是像功能不存在。
- **顶部两个图标（图表 / 卡片）**：都指向还不存在的页面。同上，不画通向死路的入口。

**折叠之后小计还在**

折叠的用意是"这组我暂时不看明细"，不是"这组不存在"。
合计跟着消失的话，折叠就等于把信息藏起来，没人会用它。

**遮金额只在这一次停留里有效**

切走再回来又是显示的。存成持久设置也行，但"默认显示"才是安全的那一边。

**顺带**

- 账户图标复用了 `CategoryIconPicker`：它只认 emoji / builtin / file:// 三种写法，
  不认识"分类"这个概念，名字里的 Category 是历史包袱不是约束
- 排序常量改成 `accountsOrder(alias)` 函数：listAccountBalances 要 JOIN，列名得写成 `a.createdAt`，
  做成函数而不是两个常量，排序规则就只有一处定义

---

## 2026-09-22 账户分组变成用户自己建的数据；新建/编辑账户独立成页

> **分组那部分已作废**，被下面「账户去掉分类」那条推翻。仍然有效的是后半段：
> 新建/编辑账户独立成页、删账户时账单转到「不选择任何账户」。

**改了什么**

- 新增 `account_groups` 表（本地 v6 迁移 + Prisma model）：分组从固定枚举变成可增删的数据
- `accounts` 加三列：`groupId`、`cardNumber`（卡号后四位）、`color`（卡片配色）
- 新建/编辑账户从弹层改成整页 `/account-editor`，带上 id 就是编辑模式
- 选分组的弹层右上角可以就地新建分组
- 资产页的分组改按 `account_groups` 排，`groupId` 为空的收进「未分组」

**分组和类型并存，不是取代**

`AccountType` 那五个枚举值留着，账户的 `type` 也继续写。两个理由：
① 本地表的 `type` 有 CHECK 约束，放不下「投资理财」这种自定义组名；
② 后端 Prisma 里 `AccountType` 是枚举，服务器要按粗粒度分类做统计仍然靠它。

所以每个**分组**自带一个 type，账户的 type 从它所属的分组那里取。
枚举退到系统层（统计口径、默认配色），用户看到的只有分组。
用户自建的分组一律算 OTHER——猜不出「投资理财」该算哪一种，就老实说"其他"，
而不是硬塞进一个看起来像的。

**老库怎么升上来**

`groupId` 必须可空：ALTER TABLE 加带外键的列，SQLite 要求默认值是 NULL。
存量账户的 groupId 在代码里按 type 回填（UUID 在 SQL 迁移里生成不了，同 seedDefaultAccounts）。
回填按 `groupId IS NULL` 找，跑过一次之后就查不到行了。

内置分组只在**空表**时灌，跟 seedDefaultCategories 一个规矩：
用户把「投资理财」删了，不该每次启动又长回来。

**删分组的顺序不能反**

`deleteAccountGroup` 必须先把组内账户的 groupId 置空，再删分组行——
外键约束开着的时候顺序反了会直接报 FOREIGN KEY constraint failed（拿真 SQLite 验过两种顺序）。
整段包在事务里。账户本身不跟着删，掉进「未分组」——
跟删账户时账单转到「不选择任何账户」是同一个道理：删的是**容器**，不是里面的东西。

**为什么新建账户是整页不是弹层**

字段多到要滚动（名称、分组、币种、期初余额、图标、配色、卡号），弹层装不下还要滚两层。
而且"进去做完事再出来"本来就该是一屏，跟记一笔同一个路子、同一种 push 转场。

**新建分组的入口放在选择器里**

不另开「分组管理」页：用户会想到要建新分组的**唯一时刻**，就是他正在选分组、
发现列表里没有他要的那个。把他赶去另一个页面建完再回来，中间那一跳会让他忘了本来在干嘛。
建完直接选中它，再让他点一下是多余的。

**没做的两块，以及为什么**

- **币种选择器**：整个 App 现在是单币种（MYR），放一个只有一个选项的选择器等于让人白点一次。
  那一行画成不可点的只读行，等做多币种时再接上。
- **「信贷账户」开关**：参考图里它跟分组是两个东西，但在我们的模型里重复了——
  「信贷账户」已经是一个分组，而"算不算负债"是按**余额正负**判断的（见上一条决策），
  不需要再多一个开关。多一个不影响任何计算的开关，只会让人以为它有用。

**卡号只存后四位**

输入框限 4 位。完整卡号是敏感信息，而这个字段的用途只是"区分同一家银行的两张卡"，
后四位就够了——存全的话是在给自己找麻烦（同步、泄露面），换不来任何额外价值。

---

## 2026-09-22 资产砍回最低限度：一个账户就四个字段

**改了什么**

把同一天早些时候堆上去的东西又拆掉了。现在资产只剩：

- 资产页 = 按分类排开的账户列表，每行「名字 + 余额」，右上角新建
- 新建/编辑账户 = 四个字段：名称、分类、期初余额、币种

**删掉的**：净资产卡（总资产/总负债）、已报销/未报销卡、分类小计、可折叠分组、
卡片预览（图标/配色/卡号）、遮金额开关。对应的 `cardNumber`、`color` 两列
从本地 v6 迁移和 Prisma 里一并去掉了——它们是今天刚加的、还没发布，不算改动已发布的迁移。

**为什么**

账户在这个 App 里只有一个职责：**让一笔账能记下"我用什么付的"**。

围着它长出来的那些汇总看着像功能，但：
- 「本月花了多少」首页已经回答过，资产页再答一遍不会更准
- 「净资产」只在用户认真维护了每个账户的期初余额之后才成立——而维护那个，
  正是这套东西最劝退的部分（同一天前面那条决策就是为这个问题来回改了两次）

一个需要先付出维护成本、才能给出一个别处已有答案的界面，是净负值。

**余额还是留着显示**

期初余额这个字段存在的全部意义就是让每行那个数字有个起点。
不显示的话，那个输入框就成了填完再也看不到的东西。
但行里不再写「余额 / 净流水」这类标签了：一行一个数，不需要再解释它是什么。

**币种留着但不给选**

全 App 记账都按 MYR 存（`transactions.currency` 固定 'MYR'、汇率固定 1）。
放一个选择器的话，选了 USD 的账户会拿一堆 MYR 的流水算出余额——数字是错的，
而界面上完全看不出来。所以那一行是只读的，等真做多币种（记账时存汇率）再打开。

顺带：`updateAccount` 不再更新 `currency`。界面只读的字段跟着 input 写，
会让没传这个字段的调用方把它默默重置成 MYR。

**留下来的**

分类（`account_groups`）留着，因为用户明确要"创建账户时选一个分类"；
删账户时账单转到「不选择任何账户」也留着——那不是附加功能，是删除这个动作本身的正确语义。

---

## 2026-09-22 账户去掉分类：一个账户就是一个名字

**改了什么**

`AccountGroup` 整套拿掉：删了 `db/account-groups.ts`、`use-account-groups.ts`、
分类选择器、资产页的分段、以及 `use-assets-view-data.ts`。
Prisma 里的 `AccountGroup` 模型和 `Account.groupId` 也退回去了。

现在：资产页是一个平铺列表；新建账户只有「名称 + 期初余额」，币种是只读的一行。

**为什么**

账户在这个 App 里的职责只有一个：**让一笔账能记下"我用什么付的"**（现金、TNG、某张卡）。
分类是在给这些标签再分一次类——账户只有两三个的时候，五个标题底下各挂一个账户，
比平铺的列表更难读。

`accounts.type` 那一列同理，现在恒为 `OTHER`。记账页的账户选择器里那行副标题
（原来是「银行卡 · MYR」）也去掉了：类型没了之后它对每个账户都一样，币种又是单一的，
一行对所有选项都相同的副标题不提供任何信息。

**白赚的：那条 Neon 迁移不用跑了**

`schema.prisma` 退回到最后一个 migration 的形状，描述文件和真库重新对上，
`prisma migrate dev` 就不必跑了。后端那个还没写的 `/account-groups` 接口也一起不需要了——
它本来是为了同步顺序而存在的（先推分类再推账户，否则 `groupId` 外键挂掉）。

**本地那张表也删干净了，但顺序不能反（这条是踩出来的）**

v7 是两句：

```sql
ALTER TABLE accounts DROP COLUMN groupId;   -- 先拆掉子表这一头的外键
DROP TABLE IF EXISTS account_groups;        -- 再删父表
```

反过来先 `DROP TABLE` 的话，在开了 `PRAGMA foreign_keys` 的设备上会报
`FOREIGN KEY constraint failed`（父表还被子表引用着）；就算绕过去删成功了，
accounts 表定义里那句 `REFERENCES account_groups(id)` 还在、却指向一张不存在的表，
**之后每一次插入账户都会失败**。

一开始只试了「直接删表」，看它两种情况都炸，就下了「删不掉、留着空表算了」的结论——
那是错的，没试过先删列。按正确顺序两种外键设置下都干净：数据没丢、子表没断、
之后新建账户和账单都正常、`foreign_key_check` 零告警（都拿真 SQLite 跑过）。

**教训：外键出问题时先看是哪一头在引用谁，不是急着给整件事下"做不到"的结论。**

**顺带的一条规矩**：v6 建的表由 v7 来删，但 **v6 本身一个字都不改**——已经跑过 v6 的设备
user_version 是 6，改它不会重跑；而把 v6 从数组里删掉会让 `MIGRATIONS.length` 退回 5，
那台设备之后再加新迁移就会被 `currentVersion >= MIGRATIONS.length` 判成已是最新而跳过。
**版本号只能往前加，不能往回缩。** 代价只是全新安装会先建这张表、下一组再删掉，几毫秒的事。

**删掉的死代码**

`constants/account-types.ts` 整个文件（`AccountTypeLabels` 最后一个调用方没了）、
`use-assets-view-data.ts`（资产页只剩一个查询，派生层就只是多一跳）。
